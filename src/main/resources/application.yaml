spring:
  application:
    name: smartlease

  datasource:
    url: jdbc:postgresql://localhost:5432/smartlease
    username: pavanpasupuleti
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


server:
  port: 8080

jwt:
  secret: mySecretKeyForSmartLeaseProject2026MySecretKey123456
  expiration: 86400000

app:
  upload-dir: ./uploads/properties
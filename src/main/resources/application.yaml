spring:
  application:
    name: smartlease

  datasource:
    url: jdbc:postgresql://localhost:5432/smartlease
    username: pavanpasupuleti
    password: ""

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8080
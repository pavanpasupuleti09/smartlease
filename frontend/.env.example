# API base URL used by the frontend.
# In development the Vite dev server proxies /api to http://localhost:8080
# (see vite.config.js), so this can stay as /api. Point it at a deployed
# backend (e.g. https://api.smartlease.com/api) for production builds.
VITE_API_BASE_URL=/api
